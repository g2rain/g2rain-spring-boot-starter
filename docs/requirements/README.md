# 需求记录

本目录记录需要跨提交追踪的 Starter 需求。每项需求建议使用 `YYYYMMDD-short-name.md`，至少说明：

- 背景、目标和非目标
- 影响模块、自动配置和公共 API
- 配置键、外部服务或中间件契约
- 兼容性、迁移与安全影响
- 验收标准、测试计划和文档影响

只有明确标为 `active` 的单一需求才能作为 `develop` 命令的默认执行对象。
