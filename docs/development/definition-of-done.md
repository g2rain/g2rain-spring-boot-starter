# 完成标准

- 变更范围和影响 Starter 明确，不夹带无关修改。
- 自动配置条件、用户覆盖和失败路径有测试。
- `mvn test` 通过；发布候选执行 `mvn clean verify`。
- 配置属性、元数据、公共 API、模块关系和运行流程文档已同步。
- 跨仓库依赖版本与被依赖项目的已发布版本一致。
- 未提交生成物、IDE 配置、密钥、Token 或本地 Maven settings。
- 明确记录未验证的中间件、远程服务、数据库和兼容性风险。
- 发布前 POM、README、CHANGELOG 与 Git 标签版本一致。
